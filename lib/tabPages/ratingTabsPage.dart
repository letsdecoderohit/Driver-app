import 'package:flutter/material.dart';

class RatingTabPAge extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "This is Rating Tab Page",
      ),
    );
  }
}
