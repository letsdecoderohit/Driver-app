import 'package:flutter/material.dart';

class EarningsTabPAge extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "This is Earnings Tab Page",
      ),
    );
  }
}
